# Basic Mod Template

This directory documents the structure `stark-devkit init-project` generates.

The CLI is the source of truth for scaffold output. This folder is a human-readable reference, not a copied runtime template.

